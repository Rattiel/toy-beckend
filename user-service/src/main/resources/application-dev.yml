server:
  port: 8090
  http2:
    enabled: true

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://pi.local:5432/postgres
    username: admin
    password: password

logging:
  level:
    com.demo: debug